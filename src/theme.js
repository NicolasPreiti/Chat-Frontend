import { extendTheme } from '@chakra-ui/react';

const fonts = {
  poppins: 'Poppins'
};

const colors = {
  primary: '#B264FC',
  secondary: '#523C66'
};

export default extendTheme({
  fonts,
  colors
});
